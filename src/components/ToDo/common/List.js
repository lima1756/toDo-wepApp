import React from 'react';
import PropTypes from 'prop-types';

const List = ({items}) => {
    return(<div></div>);
}

List.propTypes = {
    items: PropTypes.object.isRequired
};

export default List;